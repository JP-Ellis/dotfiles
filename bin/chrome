#!/bin/zsh

if $+commands[google-chrome-unstable] ; then
    google-chrome-unstable $@
elif $+commands[google-chrome-beta] ; then
    google-chrome-beta $@
elif $+commands[google-chrome-stable] ; then
    google-chrome-stable $@
elif $+commands[chromium] ; then
    chromium $@
fi
